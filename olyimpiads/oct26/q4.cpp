// #include '../comp.h

#include <iostream>
#include <vector>
#include <queue>
#include <map>

using namespace std;

#if 0

int n;
vector<bool> column;
vector<bool> leftdia;
vector<bool> rightdia;

void putqueen(int row){
    for(int col=0; col<n; col++){
        if(column[col]==false)
    }
}

int question(){

    cin.sync_with_stdio(0);
    cin.tie(0);

    cin >> n;
    col.resize(n);
    leftdia.resize(n);
    rightdia.resize(n);

    putqueen(0);

    return 0;
}


int main1(){
    question();
    return 0;
}
#endif